import java.util.*;
import java.lang.Math;

public class bilangan_prima{
    private static Scanner input = new Scanner(System.in);

    public static void main(String[] args) {
        int n;

        n = 2;
        while (isPrima(x)) {
            n = Integer.parseInt(input.nextLine());
            // Name = Expression;
        }
    }
    
    public static int isPrima(int inx) {
        int hasil;

        
        return hasil;
    }
}
